{
  "name": "khost",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "khost": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run ./src/index.ts",
    "test": "bun test",
    "deadcode": "knip --production --use-tsconfig-files",
    "check": "tsc --noEmit && knip"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "picocolors": "^1.1.1",
    "yaml": "^2.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "knip": "^6.3.0",
    "typescript": "^5.7.0"
  }
}
