{
  "name": "kfleet",
  "version": "0.1.0-dev",
  "type": "module",
  "bin": {
    "kfleet": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run ./src/index.ts",
    "build": "bun build ./src/index.ts --outdir ./dist --target bun",
    "test": "bun test",
    "deadcode": "knip --production --use-tsconfig-files",
    "check": "tsc --noEmit && knip && bun test"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "picocolors": "^1.1.1",
    "smol-toml": "^1.7.0",
    "yaml": "^2.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "knip": "^6.3.0",
    "typescript": "^5.7.0"
  }
}
