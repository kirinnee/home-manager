{
  "name": "klaude",
  "version": "1.0.0",
  "type": "module",
  "bin": {
    "klaude": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run ./src/index.ts",
    "test": "bun test",
    "deadcode": "knip --production --use-tsconfig-files",
    "check": "tsc --noEmit && knip"
  },
  "dependencies": {
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "knip": "^6.3.0",
    "typescript": "^5.7.0"
  }
}
